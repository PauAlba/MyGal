/**
 * Configuración del frontend
 * URLs de la API y constantes
 */

const CONFIG = {
    // URL del backend Express (ajustar según entorno)
    API_URL: 'http://localhost:3000/api',
    // URL del microservicio FastAPI (validación de imágenes)
    FASTAPI_URL: 'http://localhost:8000'
};
